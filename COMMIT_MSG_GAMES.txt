Add new games (2048, Minesweeper, Sudoku, Reaction Duel) + game UX & translation fixes

New single-player games (client-only, no server):
- 2048, Minesweeper, Sudoku with difficulty modes and polished UI.

New real-time multiplayer game:
- Reaction Duel: Socket.IO 1v1 "tap first after GO" duel, with server
  routes, Mongoose model, and rematch support.

Games hub + nav:
- Group games into Versus / Solo sections.
- Games nav item stays highlighted on every game route.

Existing-game UX (the "hassle" fix):
- Shared useGameReplay hook + in-place "Play Again" on all 7 existing
  games so a loss/win starts a fresh game without returning to the lobby.
  Mid-game refresh already reconnects via the existing socket join.

Translation fix (reported bug):
- PostCard now auto-translates AND offers manual translate for the
  reposted original post text and the repost comment, not just the
  main post text (reposts were not translating before).
