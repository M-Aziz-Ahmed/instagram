import ReactionDuelNoSSR from "@/components/ReactionDuel/ReactionDuelNoSSR";

export const metadata = {
    title: "Reaction Duel - AnonTweet",
    description: "Real-time two-player reflex duel — tap fastest after GO",
};

export default function ReactionDuelPage() {
    return <ReactionDuelNoSSR />;
}
